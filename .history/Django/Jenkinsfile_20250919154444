pipeline {
  agent any
  stages {
    stage('Build mock') { steps { echo 'Building Django image (mock)' } }
    stage('Test mock')  { steps { echo 'Running tests (mock)' } }
    stage('Push mock')  { steps { echo 'Pushing image to ECR (mock)' } }
  }
}


